// Armazena o número inteiro a ser testado
let numero = 15;

// Verifica se o resto da divisão por 2 é igual a zero
if (numero % 2 === 0) {
    console.log(`O número ${numero} é PAR!`);
} else {
    console.log(`O número ${numero} é ÍMPAR!`);
}